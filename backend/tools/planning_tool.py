def plan_tasks(goal: str):
    return [
        f"research background of {goal}",
        f"research key challenges of {goal}",
        f"code solution architecture for {goal}",
        f"summary findings of {goal}"
    ]